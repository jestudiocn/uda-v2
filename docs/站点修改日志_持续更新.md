# 站点修改日志（持续更新）

> 目的：记录从当前时点开始的所有功能新增、修改、修复，便于追溯、回归测试与上线核对。  
> 维护人：AI 协作更新（与开发改动同步维护）。

---

## 日志格式（固定）

- 日期时间：
- 模块：
- 页面/路由：
- 变更类型：`新增` / `修改` / `修复` / `优化` / `数据变更`
- 变更摘要：
- 详细说明：
- 涉及文件：
- 涉及数据表/字段：
- 是否需要迁移SQL：`是/否`（如是，写明 migration 文件）
- 测试要点：
- 影响范围与风险：
- 回滚方案（如需要）：

---

## 2026-04-30

### 1) 文档体系完善（数据库字典 + 测试索引）

- 日期时间：2026-04-30
- 模块：文档 / 全站
- 页面/路由：文档层（无业务路由）
- 变更类型：新增 + 修改
- 变更摘要：补齐全站数据库字段级字典，并新增测试速查索引文档。
- 详细说明：
  - 完成财务、系统管理、行事历、基础主数据、迁移中间表的字段级补全。
  - 新增按左侧菜单组织的“业务口径速查索引 Top10”。
  - 新增“测试版索引（含页面入口路径）”，用于测试逐项核对。
- 涉及文件：
  - `docs/全站数据库字典_英文名-标准中文名-业务叫法.md`
  - `docs/业务口径速查索引_TOP10.md`
  - `docs/业务口径速查索引_TOP10_测试版(含页面路径).md`
- 涉及数据表/字段：文档变更，不涉及实际库结构改动。
- 是否需要迁移SQL：否
- 测试要点：
  - 抽查各菜单 Top10 字段与页面显示口径一致。
  - 抽查页面入口路径可直达对应功能。
- 影响范围与风险：仅文档层，无运行风险。
- 回滚方案（如需要）：删除新增文档，或回退文档版本。

### 2) 建立持续日志机制

- 日期时间：2026-04-30
- 模块：文档治理
- 页面/路由：文档层（无业务路由）
- 变更类型：新增 + 修改
- 变更摘要：新增持续更新日志，并把“三文档同步维护”写入数据库字典维护约定。
- 详细说明：
  - 新增本文件作为后续所有需求修改的统一记录入口。
  - 在数据库字典维护约定中明确：后续每次改动同步更新三份文档（字典、测试索引、变更日志）。
- 涉及文件：
  - `docs/站点修改日志_持续更新.md`
  - `docs/全站数据库字典_英文名-标准中文名-业务叫法.md`
- 涉及数据表/字段：无
- 是否需要迁移SQL：否
- 测试要点：无
- 影响范围与风险：仅文档流程，无运行风险。
- 回滚方案（如需要）：删除本文件并恢复维护约定。

### 3) 全站UI统一改版（标题/按钮/弹窗/分页/派送标题）

- 日期时间：2026-04-30
- 模块：全站 UI / 派送业务
- 页面/路由：全站（主布局、登录布局、派送相关页面、打印页、司机页）
- 变更类型：修改 + 优化
- 变更摘要：按统一规范完成全站页面行为与视觉规则调整，不涉及数据库。
- 详细说明：
  - 浏览器标题统一改为 `UDA-V2 内部管理系统`（主站、登录、司机页、打印页）。
  - 全站页面标题下方说明小字统一隐藏（`h2` 后紧邻 `muted` 说明）。
  - 全站操作按钮统一规则：
    - 编辑 -> `E`（深蓝）
    - 删除 -> `D`（红色）
    - 详情/查 -> `I`（深灰）
    - 转送/回滚 -> `F`（橄榄绿）
  - 全站分页按钮文案统一：`上一页/下一页` -> `<<` / `>>`。
  - 全站弹窗样式统一为转发合包详情弹窗风格（遮罩、居中、圆角、阴影、内边距等）。
  - 派送业务二级页面标题由 `派送 / XXXX` 统一为 `派送业务 / XXXX`（控制器标题 + 视图标题）。
- 涉及文件：
  - `app/Views/layouts/main.php`
  - `app/Views/auth/auth_layout.php`
  - `app/Views/dispatch/driver_run_standalone.php`
  - `app/Views/finance/ar/invoices_print_unpaid.php`
  - `app/Controllers/DispatchController.php`
  - `app/Views/dispatch/index.php`
  - `app/Views/dispatch/order_import.php`
  - `app/Views/dispatch/package_ops.php`
  - `app/Views/dispatch/consigning_clients.php`
  - `app/Views/dispatch/delivery_customers.php`
- 涉及数据表/字段：无
- 是否需要迁移SQL：否
- 测试要点：
  - 抽查各模块列表页：编辑/删除/详情/转送/回滚按钮文字与颜色是否符合规范。
  - 抽查分页页签：是否显示 `<<` / `>>`。
  - 抽查各弹窗页面：遮罩、弹窗位置、圆角和阴影是否一致。
  - 抽查派送业务二级页面：标题是否全部变为 `派送业务 / XXXX`。
- 影响范围与风险：
  - 属于全站前端样式和文案统一，覆盖面广；个别页面若依赖按钮原文进行脚本判断，需回归验证。
- 回滚方案（如需要）：
  - 回退以上文件到本次改动前版本；或仅回退 `app/Views/layouts/main.php` 中的统一脚本和样式段。

### 4) 派送业务 / 订单查询结构升级（尺寸 + 状态更新时间 + 列表/导入改造）

- 日期时间：2026-04-30
- 模块：派送业务 / 订单查询
- 页面/路由：`/dispatch`、`/dispatch/order-import`
- 变更类型：修改 + 优化 + 数据变更
- 变更摘要：新增订单长宽高字段，`delivered_at` 改为“最后状态更新时间”，并重构订单查询列表与客户编码交互弹窗。
- 详细说明：
  - 订单表新增 `length_cm` / `width_cm` / `height_cm`，用于记录长宽高（cm）。
  - 订单状态一旦变更，统一回写 `delivered_at = NOW()` 作为“最后状态更新时间”。
  - 订单导入解析新增长/宽/高/订单状态兼容；订单状态留空时默认 `待入库`。
  - 导入模板顺序改为：委托客户编码、原始单号、派送客户编号、重量、长、宽、高、体积、数量、入库批次。
  - 订单查询列表列顺序调整为：原始面单号、客户编码、微信/Line、数量、重量、长、宽、高、入库批次、入库时间、最后状态更新时间、订单状态、委托客户（绑定委托账号隐藏该列）。
  - 客户编码改为正常文字显示，并新增：
    - `E` 按钮：中间弹窗“修改客编”
    - `I` 按钮：弹窗查看派送客户详情，含“客户需求（预留）”区块
  - 搜索条件区保持两行并拉宽至接近满宽布局。
- 涉及文件：
  - `database/migrations/045_dispatch_waybill_dimensions_and_status_updated_at.sql`
  - `app/Controllers/DispatchController.php`
  - `app/Controllers/ForwardingController.php`
  - `app/Views/dispatch/index.php`
  - `app/Views/dispatch/order_import.php`
  - `docs/全站数据库字典_英文名-标准中文名-业务叫法.md`
  - `docs/业务口径速查索引_TOP10.md`
  - `docs/业务口径速查索引_TOP10_测试版(含页面路径).md`
- 涉及数据表/字段：
  - `dispatch_waybills.length_cm`
  - `dispatch_waybills.width_cm`
  - `dispatch_waybills.height_cm`
  - `dispatch_waybills.delivered_at`（口径更新为最后状态更新时间）
- 是否需要迁移SQL：是（`database/migrations/045_dispatch_waybill_dimensions_and_status_updated_at.sql`）
- 测试要点：
  - 导入模板下载字段顺序是否正确；导入空状态是否默认为待入库。
  - 各状态流转后“最后状态更新时间”是否刷新。
  - 订单查询列顺序、委托客户列显示条件、E/I 按钮弹窗是否符合要求。
  - 搜索条件区是否保持两行且占宽更充分。
- 影响范围与风险：
  - 影响派送状态相关流程、导入和查询列表；需重点回归派送/转发/绑带/派送单生成相关状态变更路径。
- 回滚方案（如需要）：
  - 回退上述控制器与视图文件；若需回滚字段，另行编写反向 migration（线上不建议直接删列）。

### 5) 订单查询UI微调 + 导入模板补列

- 日期时间：2026-04-30
- 模块：派送业务 / 订单查询
- 页面/路由：`/dispatch`、`/dispatch/order-import?export=order_csv_template`
- 变更类型：修改
- 变更摘要：按测试反馈调整按钮排布、按钮列位置与模板字段。
- 详细说明：
  - 搜索区“查询/重置/导出”移动到“每页”右侧同排。
  - 在“客户编码”和“微信/Line”之间新增空表头按钮列，放置 `E`、`I` 两按钮。
  - 调整订单查询各列最小宽度，提升表格可读性。
  - 修复导入模板缺少“订单状态”字段，现已补上并在示例行给出默认值。
- 涉及文件：
  - `app/Views/dispatch/index.php`
  - `app/Controllers/DispatchController.php`
- 涉及数据表/字段：无新增结构变更（模板字段为既有导入逻辑补齐）
- 是否需要迁移SQL：否
- 测试要点：
  - `/dispatch` 顶部搜索区按钮是否位于“每页”右侧。
  - 列顺序是否为“客户编码 -> 空列(E/I) -> 微信/Line”。
  - 下载模板后是否包含“订单状态”列。
- 影响范围与风险：低，主要为界面布局与导出模板文案。
- 回滚方案（如需要）：回退以上两个文件本次提交内容。

### 6) 订单查询数值显示精度调整

- 日期时间：2026-04-30
- 模块：派送业务 / 订单查询
- 页面/路由：`/dispatch`
- 变更类型：修改
- 变更摘要：列表中的重量（以及体积字段复用场景）统一按小数点后两位四舍五入显示。
- 详细说明：
  - 在订单查询视图新增统一格式化函数，数值显示采用 `number_format(..., 2)`。
  - 当前列表可见的重量列已按两位小数显示。
- 涉及文件：
  - `app/Views/dispatch/index.php`
- 涉及数据表/字段：`dispatch_waybills.weight_kg`（显示层）
- 是否需要迁移SQL：否
- 测试要点：列表重量显示是否固定两位小数（例如 5 -> 5.00，5.126 -> 5.13）。
- 影响范围与风险：低，仅前端显示格式调整。
- 回滚方案（如需要）：回退上述视图文件对应改动。

### 7) 订单查询数值格式与对齐再调整

- 日期时间：2026-04-30
- 模块：派送业务 / 订单查询
- 页面/路由：`/dispatch`
- 变更类型：修改
- 变更摘要：数量改为整数显示；长宽高改为两位小数；数量/重量/长/宽/高统一右对齐。
- 详细说明：
  - 新增整数格式化函数用于数量列显示（四舍五入后取整）。
  - 长、宽、高显示口径统一为两位小数。
  - 数量、重量、长、宽、高五列设置为右对齐，便于纵向比对。
- 涉及文件：
  - `app/Views/dispatch/index.php`
- 涉及数据表/字段：
  - `dispatch_waybills.quantity`
  - `dispatch_waybills.weight_kg`
  - `dispatch_waybills.length_cm`
  - `dispatch_waybills.width_cm`
  - `dispatch_waybills.height_cm`
- 是否需要迁移SQL：否
- 测试要点：
  - 数量是否只显示整数。
  - 重量/长/宽/高是否固定两位小数。
  - 以上五列是否全部右对齐。
- 影响范围与风险：低，仅显示层格式调整。
- 回滚方案（如需要）：回退上述视图文件对应改动。

### 8) 订单查询数值列标题对齐修正

- 日期时间：2026-04-30
- 模块：派送业务 / 订单查询
- 页面/路由：`/dispatch`
- 变更类型：修改
- 变更摘要：数量/重量/长/宽/高表头改为右对齐，与数值列对齐方式一致。
- 详细说明：将五个数值列标题样式统一设置 `text-align:right`。
- 涉及文件：
  - `app/Views/dispatch/index.php`
- 涉及数据表/字段：无（显示层样式）
- 是否需要迁移SQL：否
- 测试要点：确认五个数值列“表头 + 内容”都为右对齐。
- 影响范围与风险：低，仅样式修正。
- 回滚方案（如需要）：回退上述视图文件对应改动。

### 9) 新增“部分入库”状态与到件扫描件数判定

- 日期时间：2026-04-30
- 模块：派送业务 / 订单查询 / 货件操作
- 页面/路由：`/dispatch`、`/dispatch/order-import`、`/dispatch/package-ops`
- 变更类型：修改 + 数据变更
- 变更摘要：新增订单状态“部分入库”，并按“到件扫描次数 vs 订单件数”自动判定入库阶段。
- 详细说明：
  - 订单状态枚举新增 `部分入库`，并放在 `待入库` 下方。
  - 到件扫描时：
    - 若同原始单号累计扫描次数 < 订单件数，则状态置为 `部分入库`。
    - 达到/超过订单件数后，按原规则进入 `已入库` 或（OT 路线）`待转发`。
  - 新增字段 `inbound_scan_count` 记录累计扫描次数。
  - 订单导入“订单状态”下拉新增 `部分入库` 选项。
  - 自取候选排除状态新增 `部分入库`（避免未完全入库即自取）。
- 涉及文件：
  - `database/migrations/046_dispatch_waybill_inbound_scan_count.sql`
  - `app/Controllers/DispatchController.php`
  - `app/Views/dispatch/index.php`
  - `app/Views/dispatch/order_import.php`
  - `app/Views/dispatch/package_ops.php`
  - `docs/全站数据库字典_英文名-标准中文名-业务叫法.md`
- 涉及数据表/字段：
  - `dispatch_waybills.order_status`
  - `dispatch_waybills.inbound_scan_count`
  - `dispatch_waybills.quantity`
- 是否需要迁移SQL：是（`database/migrations/046_dispatch_waybill_inbound_scan_count.sql`）
- 测试要点：
  - 同一单号重复扫描且未达到件数时，状态应为 `部分入库`。
  - 扫描次数达到订单件数后，状态应切换为 `已入库`（或 OT 路线 `待转发`）。
  - 订单状态下拉与筛选可见 `部分入库`。
- 影响范围与风险：
  - 影响到件扫描状态流转，需联动回归转发、自取、绑带、派送单生成等流程。
- 回滚方案（如需要）：
  - 回退相关控制器/视图改动；数据库层可保留 `inbound_scan_count` 列不影响旧逻辑。

### 10) 派送规则补充（司机签收联动 + 列表过滤 + 扫描分流）

- 日期时间：2026-04-30
- 模块：派送业务 / 转发操作 / 司机端
- 页面/路由：`/dispatch/driver/pod-upload`、`/dispatch/ops/delivery-list`、`/dispatch/ops/binding-list`、`/dispatch/package-ops`、`/dispatch/forwarding/packages`
- 变更类型：修改
- 变更摘要：新增四条业务规则联动：签收联动已派送、派送/绑带仅正常客户、转发客户扫描即待转发、UDA路线扫描即待自取。
- 详细说明：
  - 司机上传签收照片后，同派送单下该客户所有 `已出库` 订单同步更新为 `已派送`，并刷新状态时间。
  - 派送列表/绑带列表的候选客户新增业务状态过滤：仅 `customer_state=正常` 且订单 `已入库`。
  - 到件扫描状态机补充分流：
    - 客户状态为 `转发`：达件后状态直接置 `待转发`。
    - 客户主路线为 `UDA`：达件后状态置 `待自取`。
  - 转发合包“可转发订单”候选条件补充：除 OT 路线外，也支持客户状态为 `转发` 的订单进入候选。
- 涉及文件：
  - `app/Controllers/DispatchController.php`
  - `app/Controllers/ForwardingController.php`
- 涉及数据表/字段：
  - `dispatch_waybills.order_status`
  - `dispatch_waybills.delivery_doc_no`
  - `dispatch_delivery_customers.customer_state`
  - `dispatch_delivery_customers.route_primary`
- 是否需要迁移SQL：否
- 测试要点：
  - 司机上传后对应客户在该派送单下 `已出库` 订单是否全部变 `已派送`。
  - `customer_state=正常` 且 `已入库` 才出现在派送/绑带列表。
  - `customer_state=转发` 扫描后达件时是否进入 `待转发` 并出现在可转发订单。
  - `route_primary=UDA` 扫描后达件时是否进入 `待自取`。
- 影响范围与风险：
  - 影响派送状态流转与候选清单，需要联动回归司机端、派送端、转发端流程。
- 回滚方案（如需要）：回退上述两个控制器对应改动。

### 11) 派送客户页改造（客户要求 + 列表与弹窗）

- 日期时间：2026-04-30
- 模块：派送业务 / 派送客户
- 页面/路由：`/dispatch/delivery-customers`
- 变更类型：修改 + 数据变更
- 变更摘要：新增“客户要求”字段并接入派送客户列表、编辑弹窗、详情弹窗；列表操作列新增 `I` 详情按钮。
- 详细说明：
  - 新增数据库字段 `customer_requirements`（备注、多行）用于记录客户要求。
  - 派送客户列表中“派送客户编号”改为普通文本显示（不再作为按钮）。
  - 列表表头调整为：委托客户、派送客户编号、微信 / Line、电话、地址摘要、定位、路线、小区、变更、客户状态、操作。
  - 操作列新增 `I` 按钮用于打开详情弹窗；保留编辑能力时可与编辑按钮并存。
  - 修改弹窗与详情弹窗均新增“客户要求”字段，支持断行显示/编辑。
  - 导入模板与导入别名识别同步支持“客户要求”列，导入/编辑保存均写入该字段。
- 涉及文件：
  - `database/migrations/047_dispatch_delivery_customer_requirement.sql`
  - `app/Controllers/DispatchController.php`
  - `app/Views/dispatch/delivery_customers.php`
  - `docs/全站数据库字典_英文名-标准中文名-业务叫法.md`
- 涉及数据表/字段：
  - `dispatch_delivery_customers.customer_requirements`
- 是否需要迁移SQL：是（`database/migrations/047_dispatch_delivery_customer_requirement.sql`）
- 测试要点：
  - 修改弹窗保存“客户要求”后，列表详情弹窗可看到相同内容（含换行）。
  - CSV 导入包含“客户要求”列时，新增与覆盖更新都应写入成功。
  - 列表中客户编号为文本，操作列 `I` 按钮可正常打开详情弹窗。
- 影响范围与风险：
  - 主要影响派送客户维护页及导入口径；老数据不受影响。
- 回滚方案（如需要）：
  - 回退控制器与视图改动；数据库字段可保留（不影响旧流程）。

### 12) 派送客户地址模型升级（泰国结构化地址 + 定位状态）

- 日期时间：2026-05-01
- 模块：派送业务 / 派送客户
- 页面/路由：`/dispatch/delivery-customers`
- 变更类型：修改 + 数据变更
- 变更摘要：新增泰国地址结构字段、定位状态与完整泰文地址生成，并同步导入模板、导入逻辑及编辑/详情弹窗。
- 详细说明：
  - `dispatch_delivery_customers` 新增：门牌号、路（巷）、村、Tambon、Amphoe、Province、Zipcode、完整泰文地址、定位状态。
  - 导入模板新增上述地址列与“定位状态”，导入别名识别已接入。
  - 导入与编辑保存时自动生成 `addr_th_full`（府与 Zipcode 间不加逗号）并根据主路线+定位自动归一 `geo_status`。
  - 弹窗新增结构化地址字段输入，详情弹窗新增完整泰文地址与定位状态展示。
- 涉及文件：
  - `database/migrations/048_dispatch_delivery_customer_thai_address_profile.sql`
  - `app/Controllers/DispatchController.php`
  - `app/Views/dispatch/delivery_customers.php`
  - `docs/全站数据库字典_英文名-标准中文名-业务叫法.md`
- 涉及数据表/字段：
  - `dispatch_delivery_customers.addr_house_no`
  - `dispatch_delivery_customers.addr_road_soi`
  - `dispatch_delivery_customers.addr_moo_village`
  - `dispatch_delivery_customers.addr_tambon`
  - `dispatch_delivery_customers.addr_amphoe`
  - `dispatch_delivery_customers.addr_province`
  - `dispatch_delivery_customers.addr_zipcode`
  - `dispatch_delivery_customers.addr_th_full`
  - `dispatch_delivery_customers.geo_status`
- 是否需要迁移SQL：是（`database/migrations/048_dispatch_delivery_customer_thai_address_profile.sql`）
- 测试要点：
  - 导入包含新地址字段后，可正常新增/覆盖并生成完整泰文地址。
  - 主路线为 OT/UDA 且无定位时，定位状态自动落为 `免定位(OT/UDA)`。
  - 详情弹窗可看到结构化地址、定位状态、完整泰文地址。
- 影响范围与风险：
  - 影响派送客户导入与编辑流程；旧模板仍可导入，但新字段为空。
- 回滚方案（如需要）：
  - 回退控制器/视图改动；新增字段可保留不启用。

### 13) 派送客户列表筛选与展示（定位状态 / 泰文地址 / 县区）

- 日期时间：2026-05-01
- 模块：派送业务 / 派送客户
- 页面/路由：`/dispatch/delivery-customers`
- 变更类型：修改
- 变更摘要：列表增加「完整泰文地址」「定位状态」列；搜索区增加定位状态、泰文地址关键词、县（区）模糊筛选及「只看缺失待补」快捷入口。
- 涉及文件：
  - `app/Controllers/DispatchController.php`
  - `app/Views/dispatch/delivery_customers.php`
- 是否需要迁移SQL：否（依赖既有 048 字段；未执行 048 时仅隐藏相关 UI）
- 测试要点：
  - 执行 048 后列表出现两列，筛选与快捷链接结果正确。
  - 未执行 048 时不报错、显示迁移提示且不出现新筛选项。

### 14) 泰国行政区主数据 + 编辑弹窗府/县/镇联动下拉

- 日期时间：2026-05-01
- 模块：派送业务 / 派送客户
- 页面/路由：`/dispatch/delivery-customers`，只读 JSON：`/dispatch/th-geo/provinces`、`/dispatch/th-geo/districts`、`/dispatch/th-geo/subdistricts`
- 变更类型：修改 + 数据变更
- 变更摘要：引入 `th_geo_*` 主数据表与客户表外键；编辑弹窗在种子就绪时以「英文 | 泰文」三级联动选择府/县/镇并自动带出邮编与泰文 `addr_*`；保存时写回外键与结构化地址。
- 涉及文件：`database/migrations/049_thailand_geography_master.sql`、`database/scripts/seed_thailand_geography.php`、`database/seeds/th_geo/*.json`、`routes/web.php`、`app/Controllers/DispatchController.php`、`app/Views/dispatch/delivery_customers.php`、`docs/全站数据库字典_英文名-标准中文名-业务叫法.md`
- 部署顺序：先执行 049 迁移 → 放置 JSON 种子 → 运行 `php database/scripts/seed_thailand_geography.php`。
- 测试要点：种子灌入后编辑弹窗下拉有数据；选镇保存后 `addr_tambon`/`addr_amphoe`/`addr_province`/`addr_zipcode`/`th_geo_*` 与 `addr_th_full` 正确；清空镇级选择并手填仍可保存（外键清空）。

### 15) 派送列表行内操作按钮（E / D / i）统一规范成文

- 日期时间：2026-05-01
- 模块：派送业务（多页列表）
- 变更类型：文档 + 样式
- 变更摘要：将表格行内 **E（编辑）、D（删除）、i（详情）** 的尺寸、正圆形态、色值与 HTML 结构写入独立规范，样式集中在 `app/Views/layouts/main.php`（`btn-dispatch-round`、`dispatch-row-actions`）。
- 涉及文件：`docs/派送模块_表格行操作按钮规范.md`、`app/Views/layouts/main.php`、`app/Views/dispatch/delivery_customers.php`、`app/Views/dispatch/index.php`、`app/Views/dispatch/forwarding/index.php`、`app/Views/dispatch/consigning_clients.php`
- 说明：之后新增或修改同类行内按钮，以该 Markdown 为唯一标准，避免各页内联样式分叉。

---

## 2026-05-02

### 16) 派送操作：正式单流程、列表排序、司机派送入口与操作记录

- 日期时间：2026-05-02
- 模块：派送业务 / 派送操作
- 页面/路由：
  - `/dispatch/ops/delivery-docs`（初步派送单列表）
  - `/dispatch/ops/formal-delivery-docs`（正式派送单列表）
  - `/dispatch/ops/delivery-pick-sheets`（派送单拣货表列表）
  - `/dispatch/ops/driver-deliveries`（司机派送，新增；与 `/dispatch/driver/my-deliveries` 同控制器方法）
  - `/dispatch/driver/run`（分段导航页面，无变更逻辑，仅入口来自司机派送页链接）
- 变更类型：修改 + 新增
- 变更摘要：
  - 派送操作下相关列表统一按 **主路线 → 副路线** 再按日期/单号排序；拣货表列表同步。
  - 初步派送单去掉「优化路线」「最终派送单」，仅 **生成分段导航**；完成后即视为正式派送单（工作流写 `optimized_at` / `finalized_at` / `tokens_generated_at` 并生成司机 token）。
  - 正式派送单：指派司机后 **仍保留在列表**；新增 **司机姓名**列、**已完成**（写 `driver_run_completed_at`，完成后按钮灰显）；列表上方 **预计派送日期**、**曾指派司机** 下拉筛选。
  - 新增三级菜单 **司机派送**：仅展示当前登录账号被指派且未完成整单派送的分段链接；可提交 **本单派送完成**（同字段 `driver_run_completed_at`，且校验指派人为当前用户）。
- 详细说明（操作口径）：
  - **初步派送单**：对未生成分段导航的单，点「生成分段导航」并确认 → 单进入正式流程并生成各段司机链接。
  - **正式派送单**：用日期、曾指派司机筛选；选司机点「指派」；派送结束后点「已完成」（后台人员代记）→ 该行「已完成」禁用；司机列未指派显示「—」。
  - **司机派送**：司机登录后从菜单进入 → 按单查看各段「打开第 N 段」链接（新窗）→ 全部送完点「本单派送完成」。
- 涉及文件：
  - `app/Controllers/DispatchController.php`（`opsDeliveryDocs` POST、`opsFormalDeliveryDocs` GET/POST、`opsDeliveryPickSheets` 列表 SQL、`opsDriverAssignedDocs` GET/POST 与排序；`ensureDispatchDeliveryDocWorkflowTables` / `markDispatchDeliveryDocWorkflowStep` 含 `driver_run_completed_at`）
  - `app/Views/dispatch/ops_delivery_docs.php`
  - `app/Views/dispatch/ops_formal_delivery_docs.php`
  - `app/Views/dispatch/driver_assigned_docs.php`
  - `app/Views/layouts/main.php`（派送操作子菜单：司机派送、`dispatchOpsOpen` 路径）
  - `routes/web.php`
  - `bootstrap/app.php`
  - `database/migrations/053_dispatch_workflow_driver_run_completed.sql`
  - 同步副本：`uda-v2-new/` 下上述同名路径（控制器、视图、路由、bootstrap、迁移）
- 涉及数据表/字段：
  - `dispatch_delivery_doc_workflow.driver_run_completed_at`（司机整单派送完成时间；运行时若缺列会 `ALTER` 补齐）
  - 既有：`assigned_driver_user_id`、`assigned_at`、`tokens_generated_at`、`picking_completed_at` 等
- 是否需要迁移SQL：**是**（`database/migrations/053_dispatch_workflow_driver_run_completed.sql`；与运行时 `ALTER` 二选一执行即可，避免重复可加列前判断）
- 测试要点：
  - 初步单仅一个按钮，确认后生成分段；该单从初步列表消失、出现在正式列表。
  - 正式单：筛选日期与司机；指派后仍在列表且司机列正确；点「已完成」后按钮禁用且刷新仍禁用。
  - 司机账号：仅见本人指派单的分段链接；「本单派送完成」后该单从司机派送页消失。
  - 拣货表、初步单、正式单、司机派送列表顺序符合主副路线。
- 影响范围与风险：
  - 改变「正式单」与初步单衔接方式，旧习惯依赖（1）（2）步的用户需按新流程操作。
  - 正式单列表不再隐藏已指派行，列表变长；筛选可缓解。
- 回滚方案（如需要）：
  - 回退控制器与四份视图、路由、bootstrap、菜单；数据库列可保留（不回滚 `ALTER` 一般无妨）。若需数据回滚可手动将误标的 `driver_run_completed_at` 置 `NULL`（慎用）。

### 17) 派送客户状态「异常 / 暂停」联动可派送列表、初步单解绑与司机签收限制

- 日期时间：2026-05-02
- 模块：派送业务 / 派送客户 + 司机分段派送
- 页面/路由：`/dispatch/delivery-customers`（行内改状态、编辑保存、导入覆盖）；`/dispatch/driver/run`（分段链接）；`/dispatch/driver/pod-upload`（POST）
- 变更类型：修改 + 新增（控制器私有逻辑）
- 变更摘要：
  - **已入库**且客户为「异常」「暂停」：不进入 **可派送列表**（沿用 `dispatchInboundCustomerRows` 仅 `customer_state=正常` 过滤）。
  - 将客户改为「异常」「暂停」时：对该客户下仍为 **已入库**、且派送单 **尚未生成分段司机 token**（初步阶段）的运单，**清空** `delivery_doc_no` / `dispatch_line` / `planned_delivery_date`，并 **删除** 对应 `dispatch_delivery_doc_stops` 中该客户编码的停靠行。
  - 若该客户运单已在 **正式分段**（存在 `dispatch_driver_run_tokens`）或拣货同一单据流中：司机页对该客户显示 **闪烁警示**，**关闭**照片上传与「提交签收照片」；服务端 `pod-upload` 对同客户 **拒绝**（403）。
- 详细说明（操作口径）：
  - 后台在派送客户将状态改为 **异常** 或 **暂停**（列表快捷、编辑表单、导入均可触发）后，初步阶段绑定自动解除；已分段单据不在此回滚运单，由司机端提示与禁传签收约束行为。
- 涉及文件：
  - `app/Controllers/DispatchController.php`（`detachInboundWaybillsFromPreliminaryDispatchForDeliveryCustomer`、`applyPodBlockedFlagsForDriverSegment`、`driverRunIsPodUploadBlockedForCustomer`；在 `delivery_customer_state_update`、`save_delivery_customer_edit`、`updateDeliveryCustomerRowFromImport` 成功写库后调用解绑；`opsDriverRun` / `opsDriverPodUpload` 衔接）
  - `app/Views/dispatch/driver_run_standalone.php`（警示样式与禁用的上传区）
  - 同步：`uda-v2-new/` 同名文件
- 涉及数据表/字段：`dispatch_waybills.delivery_doc_no` 等清空；`dispatch_delivery_doc_stops` 按单号+客户编码删除行；`dispatch_delivery_customers.customer_state`；`dispatch_driver_run_tokens` 仅作「已分段」判定
- 是否需要迁移SQL：否
- 测试要点：
  - 正常→异常：已入库仅在初步单的货件从单号解绑；可派送列表不再出现该客户聚合行。
  - 已生成分段 token 的单：司机打开链接，该客户黄条闪烁，无可用上传；伪造 POST `pod-upload` 返回 403。
- 影响范围与风险：
  - 改状态会 **不可逆地解除** 初步派送绑定（需重新分配派送单）；正式在途单需人工协调货物流向。
- 回滚方案（如需要）：
  - 回退上述控制器与司机视图；误解绑数据需从备份或审计恢复 `dispatch_waybills` 派送单字段及停靠表。

### 17.1) 修复：行内切换客户状态弹「网络错误」但已保存

- 日期时间：2026-05-02
- 模块：派送业务 / 派送客户
- 页面/路由：`/dispatch/delivery-customers`（POST `delivery_customer_state_update`）
- 变更类型：修复
- 变更摘要：`mysqli` 严格模式下初步单解绑可能抛错，响应变为非 JSON，前端 `r.json()` 失败误报「网络错误」。对解绑包 `try/catch`、JSON 分支前清空输出缓冲；前端改为 `text`+`JSON.parse` 且成功时与失败时均刷新列表。
- 涉及文件：`app/Controllers/DispatchController.php`、`app/Views/dispatch/delivery_customers.php`；同步 `uda-v2-new`。

### 18) 修复：司机分段页 SQL 与 `BINARY` 字符集下的 COLLATE 冲突

- 日期时间：2026-05-02
- 模块：派送业务 / 司机分段派送
- 页面/路由：`/dispatch/driver/run` 等依赖 `delivery_doc_no` / `customer_code` 比较的查询
- 变更类型：修复
- 变更摘要：去掉 `... COLLATE utf8mb4_unicode_ci = ... COLLATE utf8mb4_unicode_ci` 写法，改为普通 `=` 比较，避免库中部分列为 `binary` 时 MySQL 报错：`COLLATION 'utf8mb4_unicode_ci' is not valid for CHARACTER SET 'binary'`。
- 涉及文件：`app/Controllers/DispatchController.php`（含 `deliveryDocHasDriverRunTokens`、`customerCodesOnDocWithRestrictedBusinessState`、工作流 JOIN、POD JOIN 等）；同步 `uda-v2-new`。
- 是否需要迁移SQL：否

### 19) 司机分段页：回标签自动刷新 + 提交前按当前客户预检

- 日期时间：2026-05-02（后续补充：同日增加 `visibilitychange` 与预检并行）
- 模块：派送业务 / 司机分段派送
- 页面/路由：`/dispatch/driver/run`；GET `/dispatch/driver/pod-precheck`（免登录，与 token 一致）
- 变更类型：新增
- 变更摘要（并行两层）：
  1. **回页刷新**：标签页曾切到后台（`visibilityState === hidden`）再回到前台时 **`location.reload()`**，整段客户列表与禁传提示与后台一致，便于司机提前看到本段整体状态变化。
  2. **提交前预检**：点击「提交签收照片」时先 `fetch` **`pod-precheck`**（仅校验**当前表单**这位 `customer_code`）；若该客户已禁传、token 无效或请求失败则刷新；通过后再原生 POST 上传（已选文件不丢）。
- 涉及文件：`app/Controllers/DispatchController.php`（`opsDriverPodPrecheck`）、`routes/web.php`、`bootstrap/app.php`（`$publicRoutes`）、`app/Views/dispatch/driver_run_standalone.php`；同步 `uda-v2-new`。
- 是否需要迁移SQL：否

### 20) 派送操作 / 绑带列表：三种状态 + 客户状态不限 + 完成后再出现

- 日期时间：2026-05-02
- 模块：派送业务 / 派送操作
- 页面/路由：`/dispatch/ops/binding-list`（`complete_binding`）
- 变更类型：修改
- 变更摘要：列表聚合「已入库」「待转发」「待自取」且未点「完成」的运单，**不按**派送客户业务状态过滤；件数为上述状态 `SUM(quantity)`；点「完成」写入 `binding_completed_at` 后从列表消失；该客户若有新扫描产生的上述状态运单（`binding_completed_at` 仍为 NULL）会再次出现。
- 涉及文件：`app/Controllers/DispatchController.php`（`dispatchBindingListCustomerRows`、`complete_binding`、`ensureDispatchWaybillBindingCompletedAt`）、`app/Views/dispatch/ops_binding_list.php`
- 涉及数据表/字段：`dispatch_waybills.binding_completed_at`（新增，与扫描写入的 `delivered_at` 解耦）
- 是否需要迁移SQL：**是** — `database/migrations/054_dispatch_waybill_binding_completed_at.sql`（首次进入相关页亦会 `ensureDispatchOrderV2` 内自动 `ALTER`）
- 测试要点：同一客户完成绑带后新增已入库/待转发/待自取运单是否重新出现；数量是否为三态之和；派送列表仍仅「正常+已入库」不受影响。
- 影响范围与风险：上线后历史运单 `binding_completed_at` 均为 NULL 时，绑带列表可能较旧逻辑**多出一批客户**；若需与历史「已完成」对齐需一次性回填或业务确认。
- 回滚方案（如需要）：回退控制器与视图；删列或忽略新列（需恢复旧查询逻辑）。

### 21) 绑带列表补充：整票再次统计时批量清除 `binding_completed_at`

- 日期时间：2026-05-02
- 模块：派送业务 / 派送操作
- 页面/路由：`/dispatch/ops/binding-list`；到件扫描、订单导入、转发推送、状态修正等写库路径
- 变更类型：修改
- 变更摘要：仅清除「当前扫描行」时，同客户其它仍为三态且曾点「完成」的运单仍带 `binding_completed_at`，绑带件数会少计。新增 **`reopenBindingListWaybillsForDeliveryCustomer`**，在到件扫描成功、导入写入三态、转发推送、状态改为三态等场景下，对该客户下三态运单及本次触发行批量 `binding_completed_at = NULL`；到件查询补充 `w.consigning_client_id`。
- 涉及文件：`app/Controllers/DispatchController.php`
- 是否需要迁移SQL：否
- 测试要点：完成绑带后同客户再扫/再导入，列表件数是否等于该客户全部三态 `SUM(quantity)`。

### 22) 订单查询 + 派送操作四页：`delivery_doc_no` / 客户编码比较 COLLATE 修复

- 日期时间：2026-05-02
- 模块：派送业务
- 页面/路由：`/dispatch`（订单列表 POD JOIN）；`/dispatch/ops/delivery-docs`、`/dispatch/ops/formal-delivery-docs`、`/dispatch/ops/delivery-pick-sheets`、`/dispatch/ops/driver-deliveries`
- 变更类型：修复
- 变更摘要：`dispatch_delivery_doc_workflow` 等为 `utf8mb4_unicode_ci`，`dispatch_waybills` / `dispatch_driver_run_tokens` 等字段常为 `general_ci`，等值比较报 **`Illegal mix of collations`**。对 workflow↔运单、token↔运单子查询的 `delivery_doc_no` 比较双方加 **`COLLATE utf8mb4_unicode_ci`**；`WHERE w.delivery_doc_no = ?` 改为列侧 `COLLATE`；订单列表 POD JOIN 同法；拣货出库 `pick_mark_outbound` 中客户编码 `TRIM` 比较加 COLLATE。
- 涉及文件：`app/Controllers/DispatchController.php`
- 是否需要迁移SQL：否

### 23) 正式派送单列表：总客户数列 + 明细弹窗（JSON）及展示调整

- 日期时间：2026-05-02
- 模块：派送业务 / 派送操作
- 页面/路由：`/dispatch/ops/formal-delivery-docs`；GET `?formal_detail_json=1&delivery_doc_no=`（JSON，与列表同权限）
- 变更类型：新增 + 修改
- 变更摘要：
  1. 列表在「预计派送日期」右侧增加 **总客户数**（`COUNT(DISTINCT consigning_client_id#客户编码)`，与初步单统计口径一致）。
  2. **明细**按钮弹窗：`formalDeliveryDocCustomerDetailRows` + JSON；列含客户编码、**微信/Line**（`CONCAT_WS('/')`）、英文泰文小区、**门牌与巷**（`, ` 合并）、**主/副线路**（`/` 合并）；弹窗内容区 `max-height: 600px`，外层 flex + `overflow-y: auto` + `scrollbar-gutter: stable`，内容超出时右侧纵向滚动。
- 涉及文件：`app/Controllers/DispatchController.php`（`formalDeliveryDocCustomerDetailRows`、`opsFormalDeliveryDocs`）、`app/Views/dispatch/ops_formal_delivery_docs.php`
- 是否需要迁移SQL：否
- 测试要点：列表总客户数与明细行数是否一致；无 `line_id` 库仅显示微信；小屏/多行时弹窗内可滚动。

### 24) 派送单：分配池过滤 + 初步单整单/增删客户 + 正式单「部份完成」

- 日期时间：2026-05-02
- 模块：派送业务 / 派送操作
- 页面/路由：`/dispatch/ops/create-delivery`（分配派送单）；`/dispatch/ops/delivery-docs`（初步派送单列表）；`/dispatch/ops/formal-delivery-docs`（正式派送单列表）
- 变更类型：修复 + 新增
- 变更摘要：
  1. **分配列表**：`dispatchInboundCustomerRows` 仅统计「已入库且 `delivery_doc_no` 为空」的货件，已绑初步或正式单的客户不再出现在「生成初步派送单」可选列表；避免再绑初步单时误 `UPDATE` 已占用运单导致正式单客户被冲掉。
  2. **初步派送单列表**：整单 **删除**（回滚池）；**调整**（统一弹窗：本单 `bind_rows` 勾选移除 + 分配池 `assign_pool_json` 勾选追加，POST `preliminary_adjust_confirm_formal` 后转入正式列表）。后台仍保留 `add_preliminary_customers` / `remove_preliminary_customers` 供兼容，列表 UI 已收敛为「调整」。
  3. **正式派送单列表**：**部份完成**（拉 `formal_undelivered_json`，POST `formal_partial_unbind_customers`），解绑仍有非「已派送」运单的客户并回滚池。
  4. `doc_customers_json` 增加 **`bind_rows`**（`preliminaryDocBindCustomerRows`），供「删除部份客户」按 `delivery_customer_id` 勾选；**不含**仅编码匹配、`delivery_customer_id` 为空的运单行时，弹窗会提示整单删除。
  5. `initializeDeliveryDocStops` 先删后插，避免无运单时残留停靠点。
- 涉及文件：`app/Controllers/DispatchController.php`（`dispatchInboundCustomerRows`、`initializeDeliveryDocStops`、`deliveryDocNotYetFormal`、`unbindWaybillsFromDeliveryDoc`、`purgeDeliveryDocAuxiliaryRows`、`preliminaryDocBindCustomerRows`、`formalDocCustomersWithNonDeliveredWaybills`、`opsDeliveryDocs`、`opsFormalDeliveryDocs`）、`app/Views/dispatch/ops_delivery_docs.php`、`app/Views/dispatch/ops_formal_delivery_docs.php`
- 涉及数据表/字段：`dispatch_waybills.delivery_doc_no` / `dispatch_line` / `planned_delivery_date`；`dispatch_delivery_doc_stops`、`dispatch_delivery_doc_workflow`、`dispatch_driver_run_tokens`（整单删除辅助行时清理）
- 是否需要迁移SQL：否（沿用既有 migration）
- 测试要点：绑初步后再看分配池是否消失；删初步整单后是否回池；再添加仅绑定仍空闲的已入库件；正式单部份完成是否只解绑勾选客户且停靠表刷新。
- 影响范围与风险：行为变更较大，操作员需知「部份完成」与「删除部份客户」均会清空运单上的派送单号/线路/日期。
- 回滚方案（如需要）：回退上述文件版本；误操作数据需按运单备份恢复 `delivery_doc_no` 等字段。
- 备注：`uda-v2-new` 已与主目录同步派送相关 `DispatchController` 与 dispatch 视图（见 **24.8**）。

### 24.2) 正式派送单列表：拣货完成后仍保留

- 日期时间：2026-05-02
- 模块：派送业务 / 派送操作
- 页面/路由：`/dispatch/ops/formal-delivery-docs`
- 变更类型：修改
- 变更摘要：`opsFormalDeliveryDocs` 列表查询去掉 `wf.picking_completed_at IS NULL` 条件；拣货表点「完成」后正式派送单不再从本列表消失。**派送单拣货表**（`/dispatch/ops/delivery-pick-sheets`）仍只展示未完成拣货的单据，逻辑不变。
- 涉及文件：`app/Controllers/DispatchController.php`、`app/Views/dispatch/ops_formal_delivery_docs.php`（页头说明）
- 是否需要迁移SQL：否

### 24.3) 正式化 / 拣货顺序：分段 → 拣货 → 指派司机

- 日期时间：2026-05-02（同日后续由 24.8 定稿 UI 与入口）
- 模块：派送业务 / 派送操作
- 页面/路由：`/dispatch/ops/formal-delivery-docs`（POST `formal_generate_driver_segments`）；`/dispatch/ops/delivery-pick-sheets`；`assign_formal_doc_driver`
- 变更类型：修改
- 变更摘要：
  1. **初步 → 正式**：在初步列表通过 **「调整」** 确认（或兼容路径 `step3_generate_segment_nav`）后调用 `preliminaryTransitionDocToFormal`：发布停靠 + `optimized_at` / `finalized_at`，**不**生成司机 token。
  2. **「仍属初步」判断**（`deliveryDocNotYetFormal`）及初步单列表筛选以 **`finalized_at` 是否为空** 为准。
  3. **正式派送单列表**：须 **`finalized_at` 非空** 才列入；先 **「生成路线分段」**（写 `tokens_generated_at`、生成司机链接），本单进入拣货表；**拣货完成**（`picking_completed_at`）后才可 **指派司机**；分段 / 指派按钮在对应步骤完成后灰显禁用。
  4. **拣货表**：以已生成分段（`tokens_generated_at`）且未完成拣货为条件列出，**不要求**已指派司机。
- 涉及文件：`app/Controllers/DispatchController.php`、`app/Views/dispatch/ops_delivery_docs.php`、`app/Views/dispatch/ops_formal_delivery_docs.php`、`app/Views/dispatch/ops_delivery_pick_sheets.php`
- 是否需要迁移SQL：否
- 测试要点：转正式后未分段时拣货表不出现；分段后出现；未拣货完成时不可指派；已指派后分段按钮已禁用等。
- 影响范围与风险：操作顺序与早期「先司机再分段」版本不同；已写历史 `tokens` 的单据仍按字段状态展示。

### 24.4) 派送单解绑回滚与客户异常/暂停：运单标为「已入库」

- 日期时间：2026-05-02
- 模块：派送业务 / 派送操作、派送客户
- 页面/路由：所有调用 `unbindWaybillsFromDeliveryDoc` 的路径；派送客户状态改为「暂停」「异常」时触发的解绑
- 变更类型：修改
- 变更摘要：
  1. **`unbindWaybillsFromDeliveryDoc`**：解绑派送单号时除清空 `delivery_doc_no` / 线路 / 日期外，若存在字段则 **`order_status = '已入库'`**，并 **`delivered_at`、`binding_completed_at` 置 NULL**（与回滚到分配池语义一致）。
  2. **`detachInboundWaybillsFromPreliminaryDispatchForDeliveryCustomer`**：改为枚举该客户下**仍绑定派送单号且非「已派送」**的运单所属 `delivery_doc_no`，对每个单号调用 `unbindWaybillsFromDeliveryDoc`（单客户 id）并在有停靠表时 **`initializeDeliveryDocStops`**；不再仅限「未 finalized 的初步单」或仅 `order_status=已入库`，以便正式单上客户异常/暂停时同样回滚并标已入库。
- 涉及文件：`app/Controllers/DispatchController.php`
- 是否需要迁移SQL：否

### 24.5) 司机分段页：同定位多客户提示（段内连续 + 跨段衔接）

- 日期时间：2026-05-02
- 模块：派送业务 / 司机端
- 页面/路由：司机免登录分段页（`opsDriverRun` 对应视图 `driver_run_standalone`）
- 变更类型：新增 + 优化
- 变更摘要：在不改变每段客户数（`array_chunk`）、`segment_index` 与拣货集包逻辑的前提下，按经纬度小数位聚合为「同址 key」：**段内**连续同 key 打组并给行加色条与「同定位 i/n」徽章；**跨段**时若本段首户与上一段末户、或本段末户与下一段首户同 key，在清单上方输出横幅文案，提醒该坐标点另有客户在本单其它段需派送。
- 详细说明：控制器 `driverRunGeoKeyForCustomerRow`、`driverRunEnrichSegmentWithSameGeoHints`；精度常量 `DRIVER_SAME_GEO_DECIMALS`（6）；缺经纬度的行不参与同址判断与跨段横幅。
- 涉及文件：`app/Controllers/DispatchController.php`、`app/Views/dispatch/driver_run_standalone.php`；副本 `uda-v2-new/app/Views/dispatch/driver_run_standalone.php`（若副本控制器未合并同逻辑，横幅与行内字段可能为空，合并控制器后一致）
- 是否需要迁移SQL：否
- 测试要点：同段相邻两户同坐标是否均标 i/n；分段边界同坐标是否出现上一段/下一段提示条；无坐标客户不报错、不出现误标。

### 24.6) 可派送列表 / 分配派送单：重货「H」窄列 + 弹窗全量可派送订单

- 日期时间：2026-05-02（同日补充：分配派送单页与可派送列表同一套逻辑）
- 模块：派送业务 / 派送操作
- 页面/路由：`/dispatch/ops/delivery-list`（`ops_delivery_list`）；`/dispatch/ops/create-delivery`（`ops_create_delivery`）
- 变更类型：新增
- 变更摘要：在「客户编码」与「微信/Line号」之间增加**无文案表头**的窄列。若该客户下任一可派送（已入库、`delivery_doc_no` 为空）运单满足 **重量 &gt; 20kg** 或 **长/宽/高中至少两项 &gt; 70cm**，则显示黑色 **「H」** 按钮；点击后以弹窗列出该客户**当前全部**可派送订单行：**原始单号、数量、重量、长、宽、高**（与列表口径一致的运单子集）。无长宽高列时按 0 参与判断与展示。
- 涉及文件：`app/Controllers/DispatchController.php`（`waybillMeetsHeavyCargoDeliveryListRule`、`dispatchInboundHeavyCargoPayloadByDeliveryCustomerIds`、`attachHeavyCargoHintsToInboundDeliveryListRows`、`opsDeliveryList`、`opsCreateDelivery`）、`app/Views/dispatch/ops_delivery_list.php`、`app/Views/dispatch/ops_create_delivery.php`；副本 `uda-v2-new` 下同名文件已同步。
- 是否需要迁移SQL：否
- 测试要点：仅轻货客户不出现 H；一单超重或两维超 70 即出现 H；弹窗行数与后台该客户可派送运单一致；JSON 转义与 XSS（单号含引号）无脚本注入。
- 影响范围与风险：列表多一次按 `dc.id IN (...)` 的批量查询；订单量极大时 HTML 体积略增（可考虑后续改 AJAX）。

### 24.7) 分配派送单成功：跳转初步派送单列表（不再停留在分配页）

- 日期时间：2026-05-02
- 模块：派送业务 / 派送操作
- 页面/路由：POST `/dispatch/ops/create-delivery`（`create_delivery_doc`）；GET `/dispatch/ops/delivery-docs`（`created_doc`、`bound` 查询参数）
- 变更类型：修改
- 变更摘要：生成初步派送单号并绑定货件成功后，使用 **303** 重定向至 **初步派送单列表**（`/dispatch/ops/delivery-docs?created_doc=单号&bound=件数`），避免操作员误以为新单在「正式派送单列表」或仍在分配页找不到单。目标页校验 `created_doc` 格式（`YYYYMMDD-[A-E]`）后展示绿色提示，并预填/打开该单号筛选与明细。
- 涉及文件：`app/Controllers/DispatchController.php`（`opsCreateDelivery`、`opsDeliveryDocs`）、`app/Views/dispatch/ops_delivery_docs.php`（页头说明）；`uda-v2-new` 同名路径已同步。
- 是否需要迁移SQL：否

### 24.8) 初步派送单「调整」弹窗与 `uda-v2-new` 控制器同步

- 日期时间：2026-05-02
- 模块：派送业务 / 派送操作
- 页面/路由：`/dispatch/ops/delivery-docs`（`doc_customers_json`、`assign_pool_json`、POST `preliminary_adjust_confirm_formal`）
- 变更类型：修改 + 文档校正
- 变更摘要：`ops_delivery_docs.php` 使用单一 `#dd-adjust-modal`，并行拉取本单绑定行与分配池，表单字段 `remove_delivery_customer_ids[]` / `add_delivery_customer_ids[]`；成功后 303 至正式列表并带 `opened_formal_doc`。分配成功页绿色提示文案改为引导「调整 → 正式 → 分段 → 拣货 → 指派」。**`uda-v2-new`** 下 `DispatchController.php` 与上述三份 dispatch 视图已与主目录 **整文件同步**，避免副本缺方法导致 500。
- 涉及文件：`app/Controllers/DispatchController.php`、`app/Views/dispatch/ops_delivery_docs.php`；`uda-v2-new/app/Controllers/DispatchController.php`、`uda-v2-new/app/Views/dispatch/ops_delivery_docs.php`、`ops_formal_delivery_docs.php`、`ops_delivery_pick_sheets.php`；`docs/站点修改日志_持续更新.md`
- 是否需要迁移SQL：否
- 测试要点：弹窗增删后正式列表可见；`unbindWaybillsFromDeliveryDoc` / `formal_partial_unbind_customers` 等回滚路径仍可走通。

### 24.1) 分配派送单：主线路 OT / UDA 客户不出列表且禁止绑定

- 日期时间：2026-05-02
- 模块：派送业务 / 派送操作
- 页面/路由：`/dispatch/ops/create-delivery`；`GET /dispatch/ops/delivery-docs?assign_pool_json=1`；POST `add_preliminary_customers`
- 变更类型：修改
- 变更摘要：`dispatchInboundCustomerRows` 增加 `UPPER(TRIM(route_primary)) NOT IN ('OT','UDA')`；生成初步派送单与「再添加」前先校验所选客户，命中则拒绝整次提交，避免伪造请求绕过列表。
- 涉及文件：`app/Controllers/DispatchController.php`
- 是否需要迁移SQL：否

### 25) UDA 集包列表：按日期号导出装箱单 Excel（PACKING LIST）

- 日期时间：2026-05-02
- 模块：UDA 快件 / 仓内操作
- 页面/路由：`/uda/batches/list`（日期号明细）；`GET /uda/batches/packing-list-export?manifest_id=`（兼容 `batch_id`）
- 变更类型：新增 + 修改
- 变更摘要：在集包列表进入某一日期号后，可一键导出该日期号下**全部集包**的装箱单 `.xlsx`；表头为 PACKING LIST，收发件人各占**一个合并单元格**并自动换行；明细中 **PRODUCT DESCRIPTION**、**QUANTITY/PCS** 留空；表末小计行与底部汇总（含净重按毛重随机系数规则）。
- 详细说明：
  1. **权限**：与集包列表一致（`menu.dispatch` / `menu.dashboard`），在 `bootstrap/app.php` 注册 `/uda/batches/packing-list-export`。
  2. **路由**：`routes/web.php` → `UdaController::manifestPackingListExport`；依赖 **PhpSpreadsheet**，未安装时返回 503 文案提示。
  3. **版面**：第 1 行标题、第 2 行右侧 DATE/B/L；第 4–18 行左侧 `A4:D18` 发件人全文、右侧 `E4:H18` 收件人全文（换行符 + `wrapText`）；第 20 行表头；按集包合并 No. / 毛重 / 宽长高列；**净重（底部 NT.WT.）** = 毛重合计 × **0.80～0.90** 间随机系数（步长 0.01，如 0.81），再 **保留两位小数**；**GR.WT.** 为各集包毛重之和；**QUANTITY** 与表内小计列 D 为**面单行数合计**（件数列未填时的统计口径）；**SHIPPING MARK** 为集包个数。
  4. **入口**：明细卡片底部按钮「导出装箱单（Excel）」；侧栏展开路径含 `packing-list-export`（`main.php`）。
  5. **副本**：`uda-v2-new` 下 `UdaController.php`、`routes/web.php`、`bootstrap/app.php`、`app/Views/uda/batches_list.php`、`app/Views/layouts/main.php` 与主站同步。
- 涉及文件：
  - `app/Controllers/UdaController.php`（`manifestPackingListExport`）
  - `routes/web.php`、`bootstrap/app.php`
  - `app/Views/uda/batches_list.php`、`app/Views/layouts/main.php`
  - `uda-v2-new/` 下上述同名路径文件
  - `docs/站点修改日志_持续更新.md`（本条及 §25 追溯）
  - `docs/业务口径速查索引_TOP10.md`、`docs/业务口径速查索引_TOP10_测试版(含页面路径).md`（§7 增补第 11 条）
  - `docs/全站数据库字典_英文名-标准中文名-业务叫法.md`（§9.18 非库字段说明）
  - `uda-v2-new/docs/` 下以上文档镜像
- 涉及数据表/字段：仅读取 `uda_manifest_batches`、`uda_manifest_bundles`、`uda_manifest_bundle_waybills`；无写库。
- 是否需要迁移SQL：否
- 测试要点：有/无面单、单集包多面单合并格是否正确；导出文件收发件人换行显示；C/D 列为空；小计 D/E 与底部 QUANTITY/GR 一致；多次导出 NT.WT. 在毛重 80%～90% 区间内变化。
- 影响范围与风险：净重为导出时随机生成，**非真实称重**，仅作单证展示用途；业务若需固定净重需改逻辑或改存库。
- 回滚方案（如需要）：移除路由、权限、`manifestPackingListExport` 与视图按钮，恢复旧行为。

### 26) UDA 集包：重量与长宽高展示统一两位小数

- 日期时间：2026-05-02
- 模块：UDA 快件 / 仓内操作
- 页面/路由：`/uda/batches/list`（日期号下各集包明细）；`/uda/batches/edit`（集包行表单默认值）；装箱单导出 `packing-list-export`
- 变更类型：修改
- 变更摘要：集包 **重量(kg)**、**长/宽/高(cm)** 在列表明细与修改页输入框中均格式化为 **小数点后 2 位**；装箱单 Excel 中 **WEIGHT(KG)**、**W/L/H(cm)** 以数值写入并对 **E 列（含小计行）、F:H 数据区** 设置数字格式 **`0.00`**，避免 Excel 浮点显示为多位小数；已移除仅用于导出的 `udaPackingListDimCell`。
- 涉及文件：`app/Views/uda/batches_list.php`、`app/Views/uda/batches_edit.php`、`app/Controllers/UdaController.php`（`manifestPackingListExport`）；`uda-v2-new` 下同名视图与控制器。
- 是否需要迁移SQL：否
- 测试要点：列表「重」「尺寸」与编辑页回填、导出 xlsx 尺寸列均为两位小数。
- 影响范围与风险：极低；仅展示与导出格式，不改存库精度。

### 27) 派送订单：签收写入派送司机姓名，已派送弹窗展示

- 日期时间：2026-05-02
- 模块：派送业务 / 订单查询 / 司机端签收
- 页面/路由：`/dispatch`（订单查询列表、已派送详情弹窗）；`POST /dispatch/driver/pod-upload`（`DispatchController::opsDriverPodUpload`）
- 变更类型：新增 + 修改
- 变更摘要：`dispatch_waybills` 新增 `delivery_driver_name`（列表不展示）；司机上传签收照并将该客户下「已出库」改为「已派送」时，从正式单工作流 `dispatch_delivery_doc_workflow.assigned_driver_user_id` 关联 `users.full_name` 写入；点击「已派送」芯片弹窗增加「派送司机」一行。
- 涉及文件：`app/Controllers/DispatchController.php`（`ensureDispatchWaybillDeliveryDriverName`、`index`、`opsDriverPodUpload`）、`app/Views/dispatch/index.php`；`uda-v2-new` 下同名文件；`database/migrations/055_dispatch_waybill_delivery_driver_name.sql`（及副本目录同名 migration）。
- 涉及数据表/字段：`dispatch_waybills.delivery_driver_name`
- 是否需要迁移SQL：是（`055_dispatch_waybill_delivery_driver_name.sql`；首次访问订单查询或上传签收时控制器亦会尝试补列）
- 测试要点：指派司机后司机端上传双照 → 订单变已派送 → 库中该运单行 `delivery_driver_name` 有值；`/dispatch` 列表无司机列；点「已派送」弹窗显示司机姓名；未指派时弹窗为「—」。
- 影响范围与风险：低；依赖 `dispatch_delivery_doc_workflow` 与 `users` 表存在且工作流有行。
- 回滚方案（如需要）：`ALTER TABLE dispatch_waybills DROP COLUMN delivery_driver_name`；回退控制器与视图改动。

### 28) 订单查询：导出列调整、筛选 UI 与最后状态更新日期间隔

- 日期时间：2026-05-02
- 模块：派送业务 / 订单查询
- 页面/路由：`/dispatch`（`DispatchController::index`、导出 `?export=current`）
- 变更类型：修改
- 变更摘要：导出 CSV 去掉长/宽/高，增加体积与派送司机；移除「扫描日期」筛选，改为「日期间隔（最后状态更新）」对应 `delivered_at` 的日期起止（可只填一侧）；系统内启用委托客户多于 1 个时才显示委托客户下拉；筛选区固定为两行（首行条件、次行每页与查询/重置/导出），窄屏横向滚动。
- 涉及文件：`app/Controllers/DispatchController.php`、`app/Views/dispatch/index.php`；`uda-v2-new` 下同名路径。
- 涉及数据表/字段：筛选条件使用 `dispatch_waybills.delivered_at`；导出读取 `volume_m3`、`delivery_driver_name`（若列不存在则导出司机列为空）。
- 是否需要迁移SQL：否
- 测试要点：仅 1 个委托客户时无客户下拉且列表仍不显示委托客户列；日期起止与列表/导出结果一致；导出表头与列顺序正确。

### 29) 订单查询：导入日期间隔筛选与第二行布局微调

- 日期时间：2026-05-02
- 模块：派送业务 / 订单查询
- 页面/路由：`/dispatch`
- 变更类型：修改
- 变更摘要：新增 `q_import_from` / `q_import_to`，按 `dispatch_waybills.import_date` 含当日区间筛选（与导出共用 WHERE）；「最后状态更新」与「导入」两组日期间隔移至第二行、位于「每页」选单左侧；第一行仅保留订单状态等其余条件。
- 涉及文件：`app/Controllers/DispatchController.php`、`app/Views/dispatch/index.php`；`uda-v2-new` 下同名路径。
- 是否需要迁移SQL：否

### 30) 订单查询：筛选区横向接近满版

- 日期时间：2026-05-02
- 模块：派送业务 / 订单查询
- 页面/路由：`/dispatch`
- 变更类型：优化
- 变更摘要：筛选表单与两行 flex 占满主内容宽度；第一行各文本/下拉等分伸展；第二行两组日期间隔等分伸展，每页条数与按钮宽度固定、操作组仍靠右；极窄屏可横向滚动。
- 涉及文件：`app/Views/dispatch/index.php`；`uda-v2-new` 同名视图。
- 是否需要迁移SQL：否

### 31) 全站双语：侧栏/顶栏键、泰语切换按钮与 t() 中文回退

- 日期时间：2026-05-02
- 模块：国际化 / 布局
- 页面/路由：全站（`layouts/main.php`、`auth/auth_layout.php`）；`bootstrap/app.php`
- 变更类型：新增 + 修改
- 变更摘要：`lang/zh-CN.php` 将 `lang.switch.th` 由「泰文」改为泰语「ไทย」；新增派送/UDA/财务/仓储/顶栏等 `nav.*` 文案并在侧栏改用 `t()`；`th-TH.php` 补齐对应泰译；泰语界面下 `t()` 若缺键则回退 `zh-CN` 包，避免裸 key；副本 `uda-v2-new` 已同步 `lang/`、`main.php`、`auth_layout.php`、`bootstrap/app.php`。
- 涉及文件：`lang/zh-CN.php`、`lang/th-TH.php`、`bootstrap/app.php`、`app/Views/layouts/main.php`、`app/Views/auth/auth_layout.php`；`uda-v2-new/` 下同名路径。
- 是否需要迁移SQL：否
- 说明：各业务子页面正文仍多为硬编码中文，需按模块逐步改为 `t('…')` 并在双语文件中补键后，泰语界面才会整页泰文。

### 32) 扫码输入稳定性与原始单号末尾 `@数字` 自动剥离

- 日期时间：2026-05-02
- 模块：UDA 批次 / 派送货件操作 / 订单查询 / 转发查询记录
- 页面/路由：`/uda/batches/create`（或等价创建批次页）、`/dispatch/package-ops`、`/dispatch`、`/dispatch/forwarding/records`
- 变更类型：修改
- 变更摘要：（1）**扫码漏字**：`batches_create` 将条码输入的回车处理由 `keydown` 改为 `keyup`，避免最后一字符未写入即提交；货件操作「到件扫描」在表单 `submit` 内用 `setTimeout(…, 0)` 延后读取输入框再提交，与到件逻辑一致的可抽函数提交。（2）**扫码头后缀**：若原始单号内容末尾为 `@` + 数字（如 `...@1`），在查询/提交前按 `replace(/@\d+$/, '').trim()` 写回输入框并用于请求，与到件扫描 `normalizeTrackingNo` 一致；覆盖：货件状态修正的原始单号、订单查询筛选原始单号、转发查询记录筛选原始单号。
- 涉及文件：`app/Views/uda/batches_create.php`、`app/Views/dispatch/package_ops.php`、`app/Views/dispatch/index.php`、`app/Views/dispatch/forwarding/index.php`；`uda-v2-new` 下同名路径。
- 是否需要迁移SQL：否
- 测试要点：各页输入或扫描以 `@1` 结尾的单号 → 提交后地址栏或请求体中无 `@1`；到件扫描与批次创建回车提交不丢末位字符。

